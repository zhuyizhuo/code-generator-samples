<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.generator.mapper.ZDatabaseChangeLogLockDao">

    <resultMap id="zDatabaseChangeLogLockResultMap" type="com.generator.model.ZDatabaseChangeLogLock">
        <id column="ID" property="id"/>
        <result column="LOCKED" property="locked"/>
        <result column="LOCKGRANTED" property="lockgranted"/>
        <result column="LOCKEDBY" property="lockedby"/>
    </resultMap>

  	<sql id="Where_Clause">
      <where>
        <if test="id != null">
            AND ID = #{id}
        </if>
        <if test="locked != null">
            AND LOCKED = #{locked}
        </if>
        <if test="lockgranted != null">
            AND LOCKGRANTED = #{lockgranted}
        </if>
        <if test="lockedby != null and lockedby != '' ">
            AND LOCKEDBY = #{lockedby}
        </if>
      </where>
  	</sql>

    <sql id="Base_Column_List">
        ID,
        LOCKED,
        LOCKGRANTED,
        LOCKEDBY
    </sql>

    <!-- 新增数据 -->
	<insert id="insertZDatabaseChangeLogLock" parameterType="com.generator.model.ZDatabaseChangeLogLock">
        INSERT INTO management.z_database_change_log_lock (
            <trim suffixOverrides=",">
                <if test="id != null">ID,</if>
                <if test="locked != null">LOCKED,</if>
                <if test="lockgranted != null">LOCKGRANTED,</if>
                <if test="lockedby != null">LOCKEDBY,</if>
            </trim>
        ) VALUES (
            <trim suffixOverrides=",">
                <if test="id != null">#{id,jdbcType=INTEGER},</if>
                <if test="locked != null">#{locked,jdbcType=bit},</if>
                <if test="lockgranted != null">#{lockgranted,jdbcType=TIMESTAMP},</if>
                <if test="lockedby != null">#{lockedby,jdbcType=VARCHAR},</if>
            </trim>
        )
	</insert>

    <!-- 根据主键删除数据 -->
	<delete id="deleteZDatabaseChangeLogLockByPrimaryKey" parameterType="java.lang.Integer">
        DELETE FROM management.z_database_change_log_lock
        WHERE ID = #{id} 
	</delete>

    <!-- 根据传入参数删除数据 -->
	<delete id="deleteZDatabaseChangeLogLockByWhere" parameterType="com.generator.model.ZDatabaseChangeLogLock">
        DELETE FROM management.z_database_change_log_lock
        <include refid="Where_Clause" />
	</delete>

    <!-- 根据主键更新数据 -->
    <update id="updateZDatabaseChangeLogLockByPrimaryKey" parameterType="com.generator.model.ZDatabaseChangeLogLock">
		UPDATE management.z_database_change_log_lock
		<set>
			<if test="locked != null">LOCKED = #{locked},</if>
			<if test="lockgranted != null">LOCKGRANTED = #{lockgranted},</if>
			<if test="lockedby != null">LOCKEDBY = #{lockedby},</if>
		</set>
		WHERE
		 ID = #{id}
	</update>

	<!-- 根据主键查询数据  -->
	<select id="queryZDatabaseChangeLogLockByPrimaryKey" resultMap="zDatabaseChangeLogLockResultMap" parameterType="java.lang.Integer">
        SELECT
        <include refid="Base_Column_List" />
  	     FROM management.z_database_change_log_lock
        WHERE
            ID = #{id}
    </select>

	<!-- 根据传入参数查询数据列表  -->
	<select id="queryZDatabaseChangeLogLockListByWhere" resultMap="zDatabaseChangeLogLockResultMap" parameterType="com.generator.model.ZDatabaseChangeLogLock">
        SELECT
        <include refid="Base_Column_List" />
  	     FROM management.z_database_change_log_lock
        <include refid="Where_Clause" />
    </select>

	<!-- 统计符合条件的数据数量 -->
	<select id="countByWhere" resultType="int" parameterType="com.generator.model.ZDatabaseChangeLogLock">
		SELECT COUNT(*) FROM management.z_database_change_log_lock
		<include refid="Where_Clause" />
	</select>

    <!-- 批量插入数据 -->
	<insert id="batchInsertZDatabaseChangeLogLock" parameterType="java.util.List">
        INSERT INTO management.z_database_change_log_lock (
        <trim suffixOverrides=",">
				<if test="null != list[0].id">ID,</if>
				<if test="null != list[0].locked">LOCKED,</if>
				<if test="null != list[0].lockgranted">LOCKGRANTED,</if>
				<if test="null != list[0].lockedby">LOCKEDBY,</if>
        </trim>
        ) VALUES
        <foreach collection="list" item="item" index= "index" separator =",">
            (
            <trim suffixOverrides=",">
				<if test="null != item.id">#{item.id,jdbcType=INTEGER},</if>
				<if test="null != item.locked">#{item.locked,jdbcType=bit},</if>
				<if test="null != item.lockgranted">#{item.lockgranted,jdbcType=TIMESTAMP},</if>
				<if test="null != item.lockedby">#{item.lockedby,jdbcType=VARCHAR},</if>
            </trim>
            )
        </foreach>
    </insert>
</mapper>